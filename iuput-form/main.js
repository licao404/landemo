/**
 * 
 * @lanlan
 * @date    2016-07-14 13:49:33
 * @version $Id$
 */
var $realname = $('.realname'),
	$realnameIpt = $realname.find('input'),
	$password = $('.password'),
	$passwordIpt = $password.find('input'),
	$repeat_password = $('.repeat-password'),
	$repeat_passwordIpt = $repeat_password.find('input'),
	$email = $('.email'),
	$emailIpt = $email.find('input'),
	$cellphone = $('.cellphone'),
	$cellphoneIpt = $cellphone.find('.ipt');

var $signup_list = $('.signup_list');

$(sig)


function checkIpt (argument) {
	 // body...  
}